// Footer único de la plataforma (consolidación 4.8).
// Se conserva este módulo como fachada para las páginas que ya lo importaban,
// evitando variantes visuales divergentes del pie.
export { default } from "@/components/platform/PlatformFooter";
